import styled from 'styled-components'

export const Button = styled.button`
    display: flex; 
    align-items: center;
    padding-top: 8px;    
    border: none;
    background: transparent;
    & svg {
        margin-right: 4px;
    }
`